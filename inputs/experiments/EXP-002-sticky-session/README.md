# EXP-002-sticky-session — PASS

**Hypothesis.** A single journey keeps one coherent network identity for its whole duration.

**Run.** `geoqa experiment run EXP-002 --samples 3 --geo oslo-mobile`
3 samples (3 ok, 0 failed), 2026-08-12T12:06:47.172Z → 2026-08-12T12:08:10.363Z.

| Metric | What it measures | Target | Verdict | Observed |
|---|---|---|---|---|
| `***` | Samples whose IP matched the session's first reading | ≥ 95% | **PASS** | 100.0% vs ≥ 95% |

## Notes

- Each sample held ONE session for 24s across 5 reads. The PRD asks for a 10-minute window; that is NOT what this measured. A short window can prove instability but cannot prove stability over a long journey.

## Files

- `hypothesis.json` — the claim and its acceptance targets, declared before the run.
- `configuration.json` — exactly what was executed, and on what.
- `results.jsonl` — one line per sample, written as it happened. Gitignored: it is run output, and it is regenerated by re-running the command above.
- `summary.json` — the machine-readable verdict.

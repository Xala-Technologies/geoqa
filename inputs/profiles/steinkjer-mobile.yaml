# Trondelag's inland admin town, north of Trondheim. The market a mid-Norway CDN still serves from Trondheim's edge.
id: steinkjer-mobile
label: Steinkjer, mobile
market:
  id: steinkjer
  country: NO
  city: Steinkjer
  language: nb-NO
  timezone: Europe/Oslo
  currency: NOK
  coordinates: [64.0147, 11.4942]
device:
  id: mobile
  kind: mobile
  viewport: { width: 390, height: 844 }
  # A real mobile identity WITHOUT a device descriptor — see `oslo-mobile.yaml` for the
  # measurement. `isMobile` is the only descriptor option that costs viewport control
  # (980 vs 390 on a page with no viewport meta tag), and it is the one not set here.
  userAgent: "Mozilla/5.0 (Linux; Android 14; Pixel 8) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/140.0.0.0 Mobile Safari/537.36"
  hasTouch: true
  deviceScaleFactor: 3
visitorType: anonymous

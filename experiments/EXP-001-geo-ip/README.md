# EXP-001-geo-ip — UNMEASURED

**Hypothesis.** A requested market yields an egress IP in that market's country and city.

**Run.** `geoqa experiment run EXP-001 --samples 20 --geo oslo-mobile`
20 samples (20 ok, 0 failed), 2026-08-12T11:58:04.178Z → 2026-08-12T11:58:54.300Z.

| Metric | What it measures | Target | Verdict | Observed |
|---|---|---|---|---|
| `***` | Sessions that produced any egress reading | ≥ 97% | **PASS** | 100.0% vs ≥ 97% |
| `***` | Sessions whose egress country matched the request | ≥ 98% | **UNMEASURED** | No geo-proxy vendor is configured, so every session egressed from this machine. The target is declared but cannot be evaluated — this is `unmeasured`, not a pass. |
| `***` | Sessions whose egress city matched the request | ≥ 90% | **UNMEASURED** | No geo-proxy vendor is configured, so every session egressed from this machine. The target is declared but cannot be evaluated — this is `unmeasured`, not a pass. |
| `***` | Mean time to first byte from the identity endpoint | ≤ 3000ms | **PASS** | 2303ms vs ≤ 3000ms |

## Notes

- 1 distinct egress IP(s) across 20 connected sample(s): 213.52.15.251
- No geo-proxy vendor is configured, so every session egressed from this machine. The target is declared but cannot be evaluated — this is `unmeasured`, not a pass.
- Baseline only — observed country NO, city Lysaker. Recorded, but it answers "where is this machine", not "can we reach a requested market".

## Files

- `hypothesis.json` — the claim and its acceptance targets, declared before the run.
- `configuration.json` — exactly what was executed, and on what.
- `results.jsonl` — one line per sample, written as it happened. Gitignored: it is run output, and it is regenerated by re-running the command above.
- `summary.json` — the machine-readable verdict.
